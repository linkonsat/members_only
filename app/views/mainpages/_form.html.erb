<%= form_with(model: mainpage) do |form| %>
  <% if mainpage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mainpage.errors.count, "error") %> prohibited this mainpage from being saved:</h2>

      <ul>
        <% mainpage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
